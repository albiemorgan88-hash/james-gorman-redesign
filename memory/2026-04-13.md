## 2026-04-13
- UK Trade Jobs was pushed live and verified returning 200 on `/`, `/careers`, `/careers/how-to-become-a-retrofit-assessor`, and `/careers/solar-panel-installer-salary-uk`.
- UKTJ deploy note: non-blocking React hook warning still exists on `/cv-builder/page.tsx`, but production deploy/build was clean.
- James Gorman next idea under consideration: a free property valuation lead hook for Derry. Ahrefs check in chat showed very low exact local search demand (`property valuation derry` 0, `house valuation derry` 0), so treat it as a seller-lead conversion play first, not a quick rankings win.
- Recommended James approach: build a proper `Free Property Valuation Derry` landing page with tool/form + local content, internal links, FAQs, and CTA, rather than relying on a thin tool embed.
## 2026-04-13
- UK Trade Jobs was pushed live and verified returning 200 on `/`, `/careers`, `/careers/how-to-become-a-retrofit-assessor`, and `/careers/solar-panel-installer-salary-uk`.
- UKTJ deploy note: non-blocking React hook warning still exists on `/cv-builder/page.tsx`, but production deploy/build was clean.
- James Gorman next idea under consideration: a free property valuation lead hook for Derry. Ahrefs check in chat showed very low exact local search demand (`property valuation derry` 0, `house valuation derry` 0), so treat it as a seller-lead conversion play first, not a quick rankings win.
- Recommended James approach: build a proper `Free Property Valuation Derry` landing page with tool/form + local content, internal links, FAQs, and CTA, rather than relying on a thin tool embed.
- Ardmore Cricket Club: a future-only CricketEurope Ardmore feed was built locally. It matches only CricketEurope North West newsroom items whose headline or teaser mentions `Ardmore`, uses teaser + source link only (not full article text), and applies a hard launch cutoff of 2026-04-13 19:39 BST so only new mentions from that point onward appear.
- Ardmore feed implementation touched `projects/ardmore-cricket/src/lib/news.ts`, `src/components/NewsArchive.tsx`, `src/app/page.tsx`, and `src/app/news/page.tsx`; local build and local checks for `/` and `/news` passed.
- Ardmore feed was intentionally NOT deployed because the repo had substantial unrelated local changes and deploying from that working tree risked shipping out-of-scope work. User later clarified an accidental `/news` message; nothing from this feed work was pushed live.
